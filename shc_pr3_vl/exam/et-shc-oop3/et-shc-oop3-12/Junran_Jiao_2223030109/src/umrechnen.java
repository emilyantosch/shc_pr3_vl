import static java.lang.StringUTF16.charAt;

public class umrechnen {
    private String str;
    private char 'I' = 1;//der Wert wird die Symbol eingeben
    private char 'V' = 5;
    private char 'X' = 10;
    private char 'L' = 50;
    private char 'C' = 100;
    private char 'D' = 500;
    private char 'M' = 1000;
    static int sum = 0;

public static int Umrechnen(String str){
    String.format(int i)]{
            return ;

    }
    for(int index = 0; index<String.length ; index++){//String iteriert
        charAt(index);
        if(charAt(index) > charAt(index+1)){
            sum +=  charAt(index);
        }else {
            sum = charAt(index+1) - charAt(index);
        }
    }
    return sum;
}

    private static int charAt(int index) {
        return 0;
    }
}