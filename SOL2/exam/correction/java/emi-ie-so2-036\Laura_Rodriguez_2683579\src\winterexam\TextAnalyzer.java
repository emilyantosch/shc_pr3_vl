package winterexam;

public class TextAnalyzer {

    int count;
    String text = "Hello world from Java";
    String[] words = text.split("");

    Public TextAnalyzer(String word) {
        this.word = word;
    }

    public String to

    String() {
        return words.tolowerCase);
    }

    public String to

    String() {
        String.replace
    }

    // Getter format
    String getX() {
        return x;
    }

    // Setter format
    public void setName(String Name) {
        this.name = name;
    }

    public static void countWords(String text)
    // for (int i = 0; i  i ++)
    {

        public static void countCharacters (String text){

    }
        public static void findLongestWord (String text){

    }
        public static void countWordOccurances (String text, String word){

    }

    }

    public static void main(String[] args) {
        countWords();
        countCharacters();
        findLongestWord();
        countWordoccurances();
        system.out.println("Number of words: " + words.length);
        system.out.println("Number of words: " + String.tolowerCase);
        system.out.println("Number of words: " + String.replace);

        switch () {
            case 1:
                countWords();
                break;
            case 2:
                countCharacters();
                break;
            case 3:
                findLongestWord();
                break;
            case 4:
                countWordoccurances();
                break;
            default:
        }
    }
}
