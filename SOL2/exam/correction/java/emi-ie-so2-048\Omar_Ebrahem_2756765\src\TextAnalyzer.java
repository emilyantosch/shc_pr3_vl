import static java.awt.SystemColor.text;
import static java.lang.StringUTF16.toLowerCase;
import static jdk.internal.joptsimple.internal.Strings.isNullOrEmpty;
import static sun.util.locale.LocaleUtils.isEmpty;

public class TextAnalyzer {


    private Object size;

    public int countWords (String text) {
        for (int i = 0; i < size; i++)
            return i;

        return 0;
    }
    public String countCharacters (String Text){

    }
    public String findLongestWord (String text){

    }
    public String findWordOccurrences (String text, String word){
        if(isNullOrEmpty (text))
            return null;
        for text t:word){
    if(text.equals(String.toLowerCase(text))
        return text;
        }
    } return null
    //display
    public void displayAlltext() {
        if(text isEmpty()){
            system.out.println("text is empy");
            return ;
        }
    }for (text t:text) system.out.print(t); }

}
 // unfortenately i had no enough time to finish