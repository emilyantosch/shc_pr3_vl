package AllyssaAzmera;

public class TextAnalyzer {
    private String words;
    private String characters;
    private String text;
}
public TextAnalyzer(String words, String characters, String text){
    this.words = words;
    this.characters = characters;
    this.text = text;
}

public static void countWords(String text) {
    return text.split("").length;}

    public static void showNumberOfWords() {
        return words.length;
        System.out.println("Number of words: words.length);
    }

public static void countCharacters(String text) {
    double sum = 0;
    for (double numberOfCharacters : numbersOfCharacters) {
        sum += numberOfCharacters;
        System.out.println("Number of characters: numberOfCharacters.length);
    }
    return numberOfCharacters.length;
}
public void countWordOccurences(String text,String word) {

    return text

    return words;
}