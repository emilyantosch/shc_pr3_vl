//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
// click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter.
import java.util.HashMap;
import java.util.Map;

public Roemischzuinteger
    public static void main(String[] args) {
      s RoemischZuInteger {
              static int roemischzuinteger(string  roemisch)
            mischeZahlen.put ('I', 1);
            mischeZahlen.put ('V', 5);
            mischeZahlen.put ('X', 10);
            mischeZahlen.put ('L', 50);
            mischeZahlen.put ('C', 100);
            mischeZahlen.put ('D', 500);
            mischeZahlen.put ('M', 1000);
            gesamt = 0;
            vorherigerWert = 0;
            (int i = roemisch.length() -1; i>=
                    char aktuelleZiffer  = roemische.charA
            new roemischeZahlen
            if (aktuellerWert < vorherigerWert)
                gesamt -= aktuellerWert;
            { else
                    gesamt += aktuellerWert;
                vorherigerWert = aktuellerWert;
                urn gasamt
